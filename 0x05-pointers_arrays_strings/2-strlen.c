#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - return the lenght of a string
 * @s: the string
 *
 * Return: lenght of the string
 */
int _strlen(char *s)
{
	int i;
	for(i = 0; s[i] != '\0'; i++)
	{
		continue;
	}

	return i;
}
